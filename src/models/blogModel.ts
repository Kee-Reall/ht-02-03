export interface blogViewModel {
    id: string
    name: string
    description: string
    websiteUrl: string
}

export interface blogInputModel {
    name: string
    description: string
    websiteUrl: string
}