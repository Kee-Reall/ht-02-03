export interface postViewModel {
    id: string
    title: string
    shortDescription: string
    content: string
    blogId: string
    blogName: string
}

export interface postInputModel {
    title: string
    shortDescription: string
    content: string
    blogId: string
}